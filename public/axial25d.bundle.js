/**
 * Axial2.5D Browser Bundle
 * Self-contained browser version of the framework
 */

// This file will be generated by the build process
// For now, it's a placeholder that loads the TypeScript output

(function() {
  'use strict';

  console.log('Axial2.5D Browser Bundle loaded');
  
  // Check if we're in a browser
  if (typeof window === 'undefined') {
    console.error('This bundle is for browser use only');
    return;
  }

  // The actual framework code will be loaded via script tags
  // This file just provides a namespace and initialization
  window.Axial25D = {
    version: '1.0.0',
    loaded: true
  };

})();
